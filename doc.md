# 安装 cnpm

```
npm install -g cnpm --registry=https://registry.npm.taobao.org
# 这样就可以使用 cnpm 命令来安装模块
cnpm install [name]
# 升级或安装 cnpm
npm install cnpm -g
# 安装全局vue-cli
cnpm install --global vue-cli
# 创建一个基于webpack的新项目，一路回车即可
vue init webpack mall-demo-web
# 
cnpm install
cnpm run dev
```
